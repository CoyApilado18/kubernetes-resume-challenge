apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hellocoy.fullname" . }}-test-connection"
  labels:
    {{- include "hellocoy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hellocoy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
