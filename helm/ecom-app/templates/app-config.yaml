apiVersion: v1
data:
  DB_HOST: {{ .Values.appConfig.DB_HOST | quote }}
  DB_NAME: {{ .Values.appConfig.DB_NAME | quote }}
  FEATURE_DARK_MODE: {{ .Values.appConfig.FEATURE_DARK_MODE | quote }}
kind: ConfigMap
metadata:
  name: app-config
  labels:
    {{- include "ecom-app.labels" . | nindent 4 }}
  namespace: gha-helm